# Claude 协作约定

本文档是 LLM 写代码时的强约束。Claude Code 启动会自动加载本文件，所有自动化补丁必须遵守。

## 注释纪律（最小必要原则）

注释是代码的**第二信号源**，随时可能与实现漂移成谎言。本仓库奉行"最小必要注释"——能用命名 / 拆函数 / 结构表达的，不写注释。

### 只允许在以下场景写注释

1. **复杂约束** —— 平台限制、协议要求、外部 API 的非直观行为
   - 例：`// 公众号后台剥离 ::before/::after，唯一稳妥是 DOM 真插 inline-block`
2. **隐含业务规则** —— 不写出来就会被误改的领域知识
   - 例：`// accent 稀缺纪律：每篇最多三处（drop cap + pull-quote + 罗马数字）`
3. **非直观实现原因** —— 为什么选了"看上去更绕"的方案
   - 例：`// 不走 markdown-it-sub：默认会吃掉 ~xxx~ 与脚注的 [^n] 冲突`
4. **跨模块契约** —— 调用方 / 实现方必须同步遵守的约定
   - 例：`// titleCSS === '' 是 variant SPI 的暗号契约：让 renderer 跳过默认 title 行`

### 严禁出现的注释

- **设计过程残留**：`// 方案 A vs B / 本来想 / 曾考虑 / 改了 N 次终于 ...`
- **历史 / 迁移上下文**：`// R8 后 / Phase 2 迁移 / 旧版用 X / 已移到 Y / 曾经 ...`
  - git log 是历史的权威来源，源码里写历史等于把 PR 描述沉淀成腐化文档
- **无效 TODO / FIXME / HACK** —— 真要做就开 issue，不要在源码里挂"以后再说"的纸条
- **重复代码语义**：`// 创建实例` `const x = new X()` —— 函数名已经说清楚的，不要复述
- **调试遗留**：`// 临时打印 / 调一下 / 这里加个 log 看看`
- **实现推演 / 教程式解说**：长段叙述"先 A 再 B 然后 C"——这是代码已经表达的内容

### 改代码时的纪律

- **删除函数/字段时**，连同周围解释它的注释一起删，不要留"// 已删除 X，理由：..."这种墓志铭
- **重构后**，必须更新所有被影响的注释，或把它们删掉。"注释和代码说的不一样"比"没注释"更糟
- **新增"why"注释**前问自己：这个 why 一年后还成立吗？如果只是"这次 PR 这么做"的临时理由，别写
- **以 `//` 写的临时性说明**优先清理；保留下来的 why 注释优先升级为 JSDoc 块注释，让类型工具能消费

### 优先级排序（用命名替代注释）

需要写注释时，先按以下顺序尝试无注释方案：

1. 改一个**更精确的变量 / 函数 / 类型名**
2. **拆出一个命名清晰的小函数**承载意图
3. 用**类型 / 断言 / schema** 把约束编码到编译期
4. 实在表达不出来，再写注释——尽量短，单行优于多行

## 其它约定

详见 [CONTRIBUTING.md](CONTRIBUTING.md)：本地开发命令、PR 自检清单、不可破坏的硬约束（写作契约 / 微信粘贴兼容性 / 预览=剪贴板 / 本地 only / 启动路径）。
