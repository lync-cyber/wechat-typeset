/**
 * recommend · card-list（默认）
 *
 * 视觉：粗体大标题 + bullet 链接列表。"延伸阅读"读者面入口——同一作者的其他文章、
 * 同号公众号推送。主题级 voice 由 ctx.containers.recommend 接管；variant 仅决定标题骨架。
 */
import type { VariantDef } from '../_core';
declare const cardList: VariantDef;
export default cardList;
