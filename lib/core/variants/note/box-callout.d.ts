/**
 * note · box-callout
 *
 * 视觉：1px 全边框 + 内边距 + 标题 textMuted。卡片感更强但仍不抢色。
 * 适合放在"参考资料 / 版本说明 / 致谢"等需要明确块界但不情绪化的位置。
 */
import type { VariantDef } from '../_core';
declare const boxCallout: VariantDef;
export default boxCallout;
