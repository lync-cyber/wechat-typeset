/**
 * divider · rule（纯 hr，最克制）
 *
 * 对应 v1 'line' 行为。wrapper 自带 margin，svgSlot 用 inline hr 避免块级 hr 的多余上下 margin。
 */
import type { VariantDef } from '../_core';
declare const rule: VariantDef;
export default rule;
