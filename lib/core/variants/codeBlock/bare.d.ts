/**
 * codeBlock · bare（默认）
 *
 * 直接产出 `<pre><code class="language-xxx hljs">...</code></pre>`，无外层 wrapper。
 * 与 v1 产物一致——升级到 v2 codeBlock 骨架后零回归。
 */
import type { CodeBlockDef } from '../_core';
declare const bare: CodeBlockDef;
export default bare;
