/**
 * sectionTitle · cornered
 *
 * 左上角装饰 SVG（theme.assets.sectionCorner）+ 标题文字，不画底线。
 * 适合想"轻装修"的主题：装饰只出现在标题行内。
 */
import type { VariantDef } from '../_core';
declare const cornered: VariantDef;
export default cornered;
