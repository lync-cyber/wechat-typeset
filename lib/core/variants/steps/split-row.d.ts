/**
 * steps · split-row
 *
 * 视觉：左侧 4px 主色实线 + 内左 hairline。利用 h3 的"较大字号 + 内左 padding-left"
 * 让作者写 `### 01 准备` 时，编号 + 标题在视觉上像"左列编号 / 右列正文"的两栏。
 * 学术 / 调研主题骨架。
 */
import type { VariantDef } from '../_core';
declare const splitRow: VariantDef;
export default splitRow;
