/**
 * steps · number-circle（默认）
 *
 * 只管 wrapper 外框 + 标题行。编号徽章 theme.assets.stepBadge(n) 由 writer 在 h3 里手工使用。
 */
import type { VariantDef } from '../_core';
declare const numberCircle: VariantDef;
export default numberCircle;
