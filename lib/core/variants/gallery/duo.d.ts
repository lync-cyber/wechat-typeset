/**
 * gallery · duo（默认）
 *
 * 人格：左右对比双联（before/after / 草图-成品 / 两幅相关画面）。
 * 骨架：display:table 50/50 两 cell，border-spacing 吸收 8px gap，比例自由。
 */
import type { VariantDef } from '../_core';
declare const duo: VariantDef;
export default duo;
